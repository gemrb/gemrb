; list of GameType values and associated local GamePath values
;
; each build it gets copied to gemrb/core/tests/resources/saves/saveTesting.ini
; under the build dir, where it can be edited without interfering with the source tree
;
; also:
; - any game types without a path assigned or save present will be skipped
; - export the environmental var IEDIFF, setting it to point to iediff from iesh
; - if iediff is not found, only a loading and resaving test will be done
; - copy any save you want to test in gemrb/tests/resources/saves/ as "0 - GameType"
bg1 = ~/games/Baldurs Gate/prefix/drive_c/GOG Games/Baldur's Gate
bg2
pst
iwd
how
iwd2
bg2ee
