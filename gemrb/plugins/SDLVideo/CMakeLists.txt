INCLUDE_DIRECTORIES( ${SDL_INCLUDE_DIR} )

ADD_GEMRB_PLUGIN (SDLVideo SDLVideo.cpp SDLVideoDC.cpp SDLVideoDriver.cpp)
TARGET_LINK_LIBRARIES( SDLVideo ${SDL_LIBRARY} )

INSTALL( TARGETS SDLVideo DESTINATION ${PLUGIN_INSTALL_DIR} )
