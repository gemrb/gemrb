INCLUDE_DIRECTORIES( ${SDL_INCLUDE_DIR} )

ADD_LIBRARY(SDLVideo MODULE SDLVideo.cpp SDLVideoDC.cpp SDLVideoDriver.cpp SDLVideoDriver.inl)
TARGET_LINK_LIBRARIES( SDLVideo gemrb_core ${SDL_LIBRARY} )

INSTALL( TARGETS SDLVideo DESTINATION ${PLUGIN_INSTALL_DIR} )