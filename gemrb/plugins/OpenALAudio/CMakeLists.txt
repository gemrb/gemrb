INCLUDE_DIRECTORIES(${OPENAL_INCLUDE_DIR} ${SDL_INCLUDE_DIR} )

FILE( GLOB OpenALAudio_files *.cpp )

ADD_LIBRARY(OpenALAudio MODULE ${OpenALAudio_files} )

TARGET_LINK_LIBRARIES(OpenALAudio gemrb_core ${OPENAL_LIBRARY} ${SDL_LIBRARY} )

INSTALL( TARGETS OpenALAudio DESTINATION ${PLUGIN_INSTALL_DIR} )
INCLUDE_DIRECTORIES(${OPENAL_INCLUDE_DIR} ${SDL_INCLUDE_DIR} )

FILE( GLOB OpenALAudio_files *.cpp )

ADD_LIBRARY(OpenALAudio MODULE ${OpenALAudio_files} )

TARGET_LINK_LIBRARIES(OpenALAudio gemrb_core ${OPENAL_LIBRARY} ${SDL_LIBRARY} )

INSTALL( TARGETS OpenALAudio DESTINATION ${PLUGIN_INSTALL_DIR} )